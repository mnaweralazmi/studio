rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // القاعدة الجديدة: السماح بالوصول الكامل (قراءة وكتابة) لجميع المسارات والبيانات
    // لأي مستخدم، سواء كان مسجلاً أم لا.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
