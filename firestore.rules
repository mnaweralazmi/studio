rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // السماح بالوصول الكامل لجميع المستخدمين لأغراض التصحيح
      allow read, write: if true;
    }
  }
}
