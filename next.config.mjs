/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'standalone' is a good default for server environments.
  // output: 'export' is for static sites and is NOT compatible with App Hosting SSR.
};

export default nextConfig;
