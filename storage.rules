rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // السماح لأي مستخدم مسجل بقراءة وكتابة الملفات
      allow read, write: if request.auth != null;
    }
  }
}
