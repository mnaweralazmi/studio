rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // السماح لأي شخص بالقراءة والكتابة دون أي شروط
      allow read, write: if true;
    }
  }
}
