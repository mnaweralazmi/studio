rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write for all authenticated users.
    // This is a simple rule for development. For production, you might want more specific rules.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
